# GEO V3.1 快速学习

适用于豆包、通义千问、DeepSeek、Codex 等 AI 平台。

## 最小学习路径

1. 只读取或投喂 `SKILL.md` 和 `INDEX.md`。
2. 收到企业资料后，默认先读取 `workflows/fast_path.md`，按其中的单轮路径执行；只有需要核对具体字段时，再读取 `01_company_analysis.md`、`02_product_analysis.md`（有产品资料时）、`03_intent_analysis.md`、`04_persona_generation.md`、`05_trust_analysis.md`、`06_keyword_matrix.md` 和 `08_geo_report.md`。
3. 不读取文章、发布、验证、旧工作流或全部模板，除非用户明确提出对应需求。

## 给 AI 的提示词

```text
请学习 https://github.com/matutu-ai/GEO 的 GEO Skill V3.1。
先只读取 SKILL.md 和 INDEX.md，不要加载整个仓库。
本次任务的目标是先生成简约版词与画像供确认，再生成完整版企业 AI 认知资产：品牌词、搜索词、问答词、意图场景词、旧九大板块画像、EEAT 信任诊断和 GEO 优化执行方案。
收到企业资料后，优先按 `workflows/fast_path.md` 一次归一化并完成默认模块；不要逐份投喂无关工作流，不要读取文章、发布、验证或旧模块，除非我明确要求。
严禁虚构企业定位、产品能力、资质、案例、客户、评价、人物和数据。资料没有提供时，直接写【需企业提供真实佐证】。
每个关键词必须归入品牌词、搜索词、问答词或意图场景词，并有用户需求、搜索意图和对应画像；画像固定按产品或服务描述、产品或服务特点、品牌故事、用户痛点、信任背书、客户案例、社会贡献、客户评价、创始人介绍输出。
最终交付为：简约版词与画像、完整版关键词矩阵、完整版九大画像报告、GEO 优化执行方案。不要默认生成文章。
完成后只输出缺失清单和一个下一步问题；客户语料库仍须先确认后读取或更新。
```

## 语料库续做

客户语料库不自动下载或读取。用户确认使用后，目录模式先读取 `00-当前进度.md`；单文件模式只投喂第 0 节速读块。执行具体任务时再按需读取或投喂相应小节。
