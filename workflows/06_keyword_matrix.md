# 06 GEO 场景词库

根据 `Company_Profile`、`Product_Profile`、`Intent_Keyword_Matrix` 输出完整版 Excel 关键词矩阵。

工作表固定为：品牌词、搜索词、问答词、意图场景词。每个工作表字段固定为：关键词、关键词类型、用户需求、搜索意图、对应画像、内容建议、优先级。

每行均需能追溯到用户意图或明确输入的企业事实；无场景来源的词不得进入导出表。
