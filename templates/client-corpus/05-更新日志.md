# {客户简称} 更新日志

> 读取规则：版本追溯、输出恢复摘要时读取。更新规则：每次语料库内容变化时，只在本文件追加一行，并升级 `00-当前进度.md` 中的版本号。

| 日期 | 更新内容 | 语料库版本 | 操作人 |
| --- | --- | --- | --- |
|  |  |  |  |

## 后续使用提示

```text
下次继续使用时，请先自动读取 {客户简称}/00-当前进度.md，恢复客户上下文，并按“任务队列”继续执行；
执行哪个任务再读取对应分文件；用户补充缺失资料后，只更新变化文件并追加本日志。
```
